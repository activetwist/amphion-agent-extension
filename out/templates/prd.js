"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.renderPrd = renderPrd;
function renderPrd(config, charter, data, timestamp) {
    const featureList = data.keyFeatures
        .split(',')
        .map(f => f.trim())
        .filter(f => f.length > 0)
        .map(f => `- ${f}`)
        .join('\n');
    return `# High-Level PRD — ${config.projectName}

Codename: \`${config.codename}\`
Version: \`${config.initialVersion}\`
Date: \`${timestamp}\`

---

## Background
**Target Users**: ${charter.targetUsers}

**Problem**: ${charter.problemStatement}

---

## Version ${config.initialVersion} Feature Set

${featureList}

---

## Success Metric

${data.successMetric}

---

## MCD Milestone Structure

| Milestone | Focus |
|---|---|
| M1 | Evaluate + Scope |
| M2 | Contract + Plan |
| M3 | Build + Verify |
| M4 | Release Readiness |

---

## Constraints
- All features are subject to contract approval before implementation
- Architecture diagrams must use Mermaid.js syntax
- No cloud dependencies or runtime package managers

---

*Generated by MCD Starter Kit · ${timestamp}*
`;
}
//# sourceMappingURL=prd.js.map